# Optional collision meshes

Keep low-poly, watertight collision meshes here when they become necessary.
The initial model uses primitive collision shapes because they are faster and
more stable for buoyancy simulation.
